# Dependência obrigatória: clientes HTTP das três APIs.
requests>=2.31

# Opcionais — só são necessárias nas rotas de IA por chave de API. A rota em
# produção (OpenClaw, provider openai/Codex) fala com o CLI por subprocess e não
# precisa de SDK. Sem nenhuma delas, o sistema usa o heurístico local.
# openai>=1.40
# anthropic>=0.40

# Opcional — só é necessária para o fluxo interativo de autorização do Google,
# rodado uma única vez por scripts/autorizar_google.py.
# google-auth-oauthlib>=1.2

# Necessária para a rota de conta de serviço da Google Agenda: é dela que sai a
# assinatura RSA do JWT. Quem usa a rota OAuth (GOOGLE_TOKEN_PATH) não precisa.
google-auth>=2.30

# Necessária para expor a Google Agenda ao agente como servidor MCP
# (scripts/openclaw/gcal_mcp.sh). Quem só usa a CLI do sop não precisa.
mcp>=2.0

# Leitura de nota de mercado (foto e PDF). São bibliotecas Python de propósito:
# a VPS não tem root, então depender de pacote de sistema (tesseract, poppler)
# deixava o fluxo de foto morto sem ninguém perceber. Com o tesseract ou o
# pdftotext instalados, eles continuam sendo usados como caminho alternativo.
rapidocr-onnxruntime>=1.4      # OCR local, modelos embutidos, sem serviço pago
opencv-python-headless>=4.9    # pré-processamento da foto antes do OCR
pypdf>=4.0                     # texto de PDF sem poppler
PyMuPDF>=1.24                  # rasteriza PDF sem camada de texto para OCR

# Desenvolvimento.
pytest>=8.0
