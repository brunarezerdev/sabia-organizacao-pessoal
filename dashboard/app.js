const money=v=>Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});let data,view;
const el=id=>document.getElementById(id);const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function summary(rows){let receitas=0,despesas=0,cats={},evo={};for(const x of rows){if(x.tipo==='Receita')receitas+=x.valor;else{despesas+=x.valor;cats[x.categoria]=(cats[x.categoria]||0)+x.valor}const m=x.data.slice(0,7);evo[m]=(evo[m]||0)+(x.tipo==='Receita'?x.valor:-x.valor)}return{receitas,despesas,saldo:receitas-despesas,cats,evo}}
function bars(obj){const max=Math.max(1,...Object.values(obj).map(Math.abs));return Object.entries(obj).map(([k,v])=>`<div class="bar"><span>${esc(k)}</span><span class="track"><i style="width:${Math.abs(v)/max*100}%"></i></span><b>${money(v)}</b></div>`).join('')||'Sem dados neste filtro.'}
function render(){let rows=data.lancamentos.filter(x=>(!el('mes').value||x.data.startsWith(el('mes').value))&&(!el('tipo').value||x.tipo===el('tipo').value)&&(!el('categoria').value||x.categoria===el('categoria').value)&&(!el('status').value||x.status===el('status').value));const s=summary(rows);for(const k of ['receitas','despesas','saldo'])el(k).textContent=money(s[k]);el('orcamento').textContent=money(data.orcamentos.reduce((a,x)=>a+x.limite,0));el('categorias').innerHTML=bars(s.cats);el('evolucao').innerHTML=bars(s.evo);el('lancamentos').innerHTML=rows.map(x=>`<tr><td>${esc(x.data)}</td><td>${esc(x.nome)}</td><td>${esc(x.tipo)}</td><td>${esc(x.categoria)}</td><td>${esc(x.status)}</td><td>${money(x.valor)}</td></tr>`).join('');}
fetch('data.json').then(r=>r.json()).then(d=>{data=d;const meses=[...new Set(d.lancamentos.map(x=>x.data.slice(0,7)))];el('mes').innerHTML+=[...meses].map(x=>`<option>${esc(x)}</option>`).join('');const cats=[...new Set(d.lancamentos.map(x=>x.categoria))];el('categoria').innerHTML+=cats.map(x=>`<option>${esc(x)}</option>`).join('');el('custos').innerHTML=d.custos.map(x=>`<div class="row"><span>${esc(x.nome)}</span><b>${money(x.valor)}</b></div>`).join('');el('orcamentos').innerHTML=d.orcamentos.map(x=>`<div class="row"><span>${esc(x.categoria)}</span><b>${money(x.realizado)} / ${money(x.limite)}</b></div>`).join('');document.querySelectorAll('select').forEach(x=>x.onchange=render);render()});
